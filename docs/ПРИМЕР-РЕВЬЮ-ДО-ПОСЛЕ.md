# Примеры кода ревью: до и после

Краткая выжимка из ревью **REV-2026-05-20-01**. Полный отчёт: [РЕВЬЮ-КОДА.md](./РЕВЬЮ-КОДА.md).

---

## 1. DEF-01 — состав заказа в панели оператора (Кол-во = 0)

**Симптом:** в `operator.html` сумма заказа верная, а «Кол-во» и «Бутылей 18,9 л» — нули.

### 1.1. Сохранение заказа (`server.js`)

**До**

```javascript
for (const it of items) {
  const p = await db.prepare('SELECT id, price, stock, hidden FROM products WHERE id = ?').get(it.product_id);
  subtotal += p.price * it.qty;
  lineItems.push({ ...it, unit_price: p.price });
}
const itemsJson = JSON.stringify({ lines: lineItems, subtotal, payable, bonusSpend });
```

**После** (текущий код, ~1871–1893)

```javascript
const p = await db
  .prepare('SELECT id, name, price, stock, hidden FROM products WHERE id = ?')
  .get(it.product_id);
const qty = Math.max(1, Math.floor(Number(it.qty) || 0));
const unit_price = Math.round(Number(p.price) || 0);
subtotal += unit_price * qty;
lineItems.push({
  title: String(p.name || 'Товар').trim(),
  qty,
  unit_price,
  product_id: it.product_id,
});
const itemsJson = JSON.stringify(lineItems);
```

### 1.2. Чтение в операторе (`operator.js`)

**До**

```javascript
function parseQty(itemsJson) {
  const items = JSON.parse(itemsJson || '[]');
  if (!Array.isArray(items)) return 0;
  return items.reduce((s, it) => s + (Number(it.qty) || 0), 0);
}
```

**После**

```1683:1693:operator.js
  function extractOrderItemsArray(itemsJson) {
    if (itemsJson == null || itemsJson === '') return [];
    try {
      const parsed = typeof itemsJson === 'string' ? JSON.parse(itemsJson) : itemsJson;
      if (Array.isArray(parsed)) return parsed;
      if (parsed && Array.isArray(parsed.lines)) return parsed.lines;
      return [];
    } catch {
      return [];
    }
  }
```

```3671:3678:operator.js
  function parseQty(itemsJson) {
    try {
      const items = extractOrderItemsArray(itemsJson);
      if (!items.length) return 0;
      return items.reduce((s, it) => s + (Number(it.qty) || 0), 0);
    } catch {
      return 0;
    }
  }
```

### 1.3. JSON в базе

| | Пример `items_json` | `parseQty()` |
|--|---------------------|--------------|
| **До** | `{"lines":[{"product_id":1,"qty":12,"unit_price":190}],"subtotal":2280}` | `0` |
| **После** | `[{"title":"ЭкваЛайн 19 л","qty":12,"unit_price":190,"product_id":1}]` | `12` |

---

## 2. DEF-02 — сессия при бездействии

**Симптом:** пользователь не трогает страницу, но фоновый опрос заказов держит вход открытым.

### 2.1. Сервер (`server.js`)

**До**

```javascript
app.use(session({
  rolling: true,
  cookie: { maxAge: 2 * 24 * 60 * 60 * 1000 },
}));

const requireAuth = asyncHandler(async (req, res, next) => {
  const uid = req.session && req.session.userId;
  if (!uid) return res.status(401).json({ error: 'Требуется вход в систему.' });
  next();
});
```

**После**

```430:467:server.js
function sessionIdleExpired(req) {
  if (!req.session?.userId) return false;
  const last = Number(req.session.lastActivityAt) || 0;
  if (!last) return false;
  return Date.now() - last > SESSION_IDLE_MS;
}

function maybeTouchSessionActivity(req) {
  if (!req.session?.userId) return;
  const method = String(req.method || 'GET').toUpperCase();
  const userActive = String(req.headers['x-user-activity'] || '') === '1';
  if (method !== 'GET' && method !== 'HEAD') {
    req.session.lastActivityAt = Date.now();
    return;
  }
  if (userActive) req.session.lastActivityAt = Date.now();
}

async function rejectIfSessionIdle(req, res) {
  if (!req.session?.userId) return false;
  if (!sessionIdleExpired(req)) return false;
  await destroySessionPromise(req);
  res.status(401).json({
    error: 'Сессия завершена из‑за бездействия. Войдите снова.',
    sessionExpired: true,
  });
  return true;
}
```

В `requireAuth` добавлен вызов `rejectIfSessionIdle` перед загрузкой пользователя.

Настройка: `.env` → `SESSION_IDLE_MINUTES=30` (по умолчанию).

### 2.2. Клиент (`api-client.js`)

**До**

```javascript
async function apiJson(path, options = {}) {
  const { r, parsed } = await doFetch();
  return parsed;
}
```

**После**

```75:81:api-client.js
  function handleSessionAuthFailure(path, status, data) {
    if (Number(status) !== 401) return;
    if (isPublicApiPath(path)) return;
    const expired = data && data.sessionExpired === true;
    if (!expired && !hadStoredUser()) return;
    redirectToLoginAfterSessionExpiry();
  }
```

```javascript
// В apiFetch: заголовок только при действиях пользователя
if (opts.markUserActivity !== false && shouldSendUserActivityHeader()) {
  headers['X-User-Activity'] = '1';
}
// Редирект: index.html?session-expired=1 → script.js открывает «Вход»
```

### 2.3. Поведение

| Ситуация | До | После |
|----------|-----|--------|
| 30 мин без кликов, фоновый `GET /api/orders/my` | Сессия остаётся | `401`, переход на вход |
| Клики / оформление заказа | Сессия жива | Сессия продлевается |

---

## Шаблон для следующего ревью

При новом DEF копируйте блок:

```markdown
### DEF-XX — краткое название

**До**
\`\`\`javascript
// фрагмент старого кода
\`\`\`

**После**
\`\`\`javascript
// фрагмент нового кода или ссылка startLine:endLine:file
\`\`\`

**Эффект:** что изменилось для пользователя / БД / безопасности.
```

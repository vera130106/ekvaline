# Архитектура веб-приложения «ЭкваЛайн»

Документ для раздела курсовой/диплома: **иерархия страниц и файлов**, **символьные схемы модулей**, **перечень функций**.

---

## Как оформить схемы в отчёте (рисунки)

1. **Откройте этот файл** в VS Code / Cursor или на [mermaid.live](https://mermaid.live).
2. Скопируйте блок кода между ` ```mermaid ` и ` ``` `.
3. На mermaid.live выберите тему **Base** (белый фон) → **Export → PNG/SVG** — вставьте в Word как **Рисунок N**.
4. Подпись под рисунком (пример): *«Рисунок 1 — Общая архитектура системы»*, *«Рисунок 2 — Процесс от регистрации до доставки»*.
5. В тексте диплома: *«Структура приложения представлена на рисунке 3»*.

Альтернатива: плагин **Markdown Preview Mermaid Support** → печать в PDF.

---

## 1. Общая архитектура (трёхзвенная)

*Рисунок 1 — как устроена система: браузер, сервер Node.js, база и почта. Подписи на русском, технические имена — на английском.*

```mermaid
%%{init: {'theme': 'base', 'themeVariables': {'background': '#ffffff', 'primaryColor': '#f0f6fc', 'primaryTextColor': '#1a1a1a', 'primaryBorderColor': '#2b5c8a', 'lineColor': '#1a3a5c', 'secondaryColor': '#ffffff', 'tertiaryColor': '#e8f0f8', 'clusterBkg': '#ffffff', 'clusterBorder': '#2b5c8a', 'fontFamily': 'Arial'}}}%%
flowchart TB
  subgraph L1["Уровень 1 — Клиент (браузер)"]
    direction TB
    UI["HTML-страницы"]
    JS["JavaScript-модули<br/>script.js, cabinet-pg.js, operator.js…"]
    LS["localStorage и cookie сессии"]
    UI --> JS
    JS --> LS
  end

  subgraph L2["Уровень 2 — Сервер приложений (Node.js Express, server.js)"]
    direction TB
    STATIC["Раздача статики<br/>CSS, HTML, JS"]
    API["REST API<br/>/api/*"]
    SEC["Сессии express-session<br/>и защита CSRF"]
    API --> SEC
  end

  subgraph L3["Уровень 3 — Внешние сервисы"]
    direction LR
    PG[("PostgreSQL")]
    SMTP["SMTP<br/>почтовый сервер"]
  end

  JS -->|"HTTP: fetch + credentials"| API
  JS -->|"HTTP: загрузка файлов"| STATIC
  API -->|"SQL-запросы"| PG
  API -->|"nodemailer"| SMTP
```

| Уровень | Технологии | Назначение |
|---------|------------|------------|
| Представление | HTML, CSS, JS | Интерфейс по ролям (клиент, оператор, водитель) |
| Логика | Express 4, модули `.js` | API, валидация, бизнес-правила |
| Данные | PostgreSQL, SMTP | Заказы, пользователи, уведомления, письма с кодами |

---

## 2. Процесс работы: клиент → оператор → водитель

*Рисунок 2 — сквозная иерархия: регистрация клиента, заявка, обработка оператором, доставка водителем. Стрелки показывают направление данных; цилиндр — PostgreSQL.*

```mermaid
%%{init: {'theme': 'base', 'themeVariables': {'background': '#ffffff', 'primaryColor': '#f0f6fc', 'primaryTextColor': '#1a1a1a', 'primaryBorderColor': '#2b5c8a', 'lineColor': '#1a3a5c', 'secondaryColor': '#ffffff', 'tertiaryColor': '#fff8e6', 'clusterBkg': '#ffffff', 'clusterBorder': '#2b5c8a', 'fontFamily': 'Arial'}}}%%
flowchart TB
  C1["1. Клиент — регистрация и вход<br/>index.html, script.js"]
  S1["server.js — /api/auth/*, mailer.js"]
  C1 -->|"HTTP POST"| S1

  C2["2. Клиент — оформление заявки<br/>catalog.html, корзина, script.js"]
  S2["server.js — POST /api/orders<br/>bonusProgram.js"]
  C2 -->|"HTTP POST"| S2

  OPR["3. Оператор — обработка заявки<br/>operator.html, operator.js"]
  S3["server.js — PATCH /api/orders/:id<br/>clientNotifications.js"]
  OPR -->|"HTTP PATCH"| S3

  DRV["4. Водитель — доставка<br/>driver.html, driver.js"]
  S4["server.js — /api/driver/orders<br/>mark-delivered"]
  DRV -->|"HTTP GET / POST"| S4

  C3["5. Клиент — кабинет и уведомления<br/>cabinet.html, notifications-client.js"]

  PG[("PostgreSQL")]

  S1 -->|"users, коды email"| PG
  S2 -->|"orders, products, bonus_operations"| PG
  S3 -->|"orders, order_audit_events,<br/>client_notifications"| PG
  S4 -->|"orders.status = delivered"| PG

  S1 -.->|"SMTP: письмо с кодом"| C1
  S3 -->|"назначение driver, статус on_way"| DRV
  PG -->|"GET /api/notifications/my"| C3
```

**Пояснение к рисунку 2 (по шагам)**

| Шаг | Кто | Действие | Запись в PostgreSQL |
|-----|-----|----------|---------------------|
| 1 | Клиент | Регистрация, подтверждение email кодом из письма | `users`, коды в `users` |
| 2 | Клиент | Заявка из каталога: адрес, дата, товары, бонусы | `orders`, `products.stock`, `bonus_operations` |
| 3 | Оператор | Подтверждение, перенос, отмена, назначение водителя | `orders`, `order_audit_events` |
| 4 | Водитель | Список маршрута, отметка «Доставлено» | `orders.status` |
| 5 | Клиент | Уведомления в колокольчике и статус в кабинете | `client_notifications` |

---

## 3. Иерархия страниц (навигация и роли)

```mermaid
flowchart TD
  ROOT["index.html — Главная"]

  subgraph Public["Публичный сайт (клиент)"]
    CAT["catalog.html — Каталог"]
    DEL["delivery.html — Доставка"]
    ABT["about.html — О компании"]
    CON["contacts.html — Контакты"]
    COM["community.html — Блог / опросы"]
    CAB["cabinet.html — Личный кабинет"]
    RST["reset-password.html — Смена пароля"]
    VEM["verify-email.html → редирект в кабинет"]
  end

  subgraph Staff["Служебные панели"]
    OPR["operator.html — Оператор"]
    MGR["manager.html — Менеджер"]
    ADM["admin.html — Администратор"]
    DRV["driver.html — Водитель"]
  end

  ROOT --> CAT
  ROOT --> DEL
  ROOT --> ABT
  ROOT --> CON
  ROOT --> COM
  ROOT --> CAB
  ROOT --> RST

  CAB -.->|"требует вход client"| ROOT
  RST -.->|"код из email"| ROOT

  ROOT -->|"вход operator/manager/admin"| OPR
  ROOT -->|"вход manager/admin"| MGR
  ROOT -->|"вход admin"| ADM
  ROOT -->|"вход driver"| DRV

  CAB --> VEM
```

**Роли и доступ к страницам**

| Страница | client | operator | manager | admin | driver |
|----------|--------|----------|---------|-------|--------|
| index, catalog, delivery, about, contacts, community | ✓ | ✓ | ✓ | ✓ | — |
| cabinet, reset-password | ✓ | — | — | — | — |
| operator | — | ✓ | ✓ | ✓ | — |
| manager | — | — | ✓ | ✓ | — |
| admin | — | — | — | ✓ | — |
| driver | — | — | — | — | ✓ |

---

## 4. Зависимости: страницы → клиентские JS/CSS

```mermaid
flowchart LR
  subgraph Pages["HTML-страницы клиента"]
    P1[index / catalog / delivery / about / contacts / community]
    P2[cabinet.html]
    P3[reset-password.html]
  end

  subgraph Core["Общие модули"]
    MN[mobile-nav.js]
    AC[app-core.js]
    API[api-client.js]
    NC[notifications-client.js]
    OS[orders-sync.js]
    MR[maps-runtime.js]
    RT["/ekvaline-runtime.js"]
    ST[styles.css + responsive-mobile.css]
  end

  subgraph PageJS["Страничная логика"]
    SCR[script.js — корзина, auth, checkout]
    CP[cabinet-pg.js — кабинет]
    RP[reset-password-page.js]
    CM[community.js]
  end

  P1 --> MN --> AC --> API --> NC
  P1 --> OS --> SCR
  P1 --> MR --> RT --> ST

  P2 --> SCR
  P2 --> CP

  P3 --> SCR
  P3 --> RP

  P1 --> CM
```

```mermaid
flowchart LR
  subgraph StaffPages["HTML панелей"]
    OP[operator.html]
    MG[manager.html]
    AD[admin.html]
    DR[driver.html]
  end

  subgraph StaffJS["JS панелей"]
    OJ[operator.js]
    MJ[manager.js]
    AJ[admin.js]
    DJ[driver.js]
  end

  OP --> API2[api-client.js] --> OJ
  MG --> API2 --> MJ
  AD --> API2 --> AJ
  DR --> API2 --> DJ
  OP --> DS[delivery-slots.js]
  OP --> OFL[opx-field-limits.js]
```

---

## 5. Зависимости сервера: `server.js` → модули

```mermaid
flowchart TD
  SRV[server.js — точка входа]

  SRV --> DB[db.js — PostgreSQL, миграции]
  SRV --> SCH[schemas.js — Joi-валидация]
  SRV --> SEC[security-middleware.js]
  SRV --> MAIL[mailer.js — SMTP]
  SRV --> BON[bonusProgram.js]
  SRV --> NOT[clientNotifications.js]
  SRV --> POL[pollVotes.js]
  SRV --> GEO[geocode-proxy.js]
  SRV --> DAV[delivery-availability.js]
  SRV --> ORE[orenburg-address.js]
  SRV --> OPR[order-pricing.js]
  SRV --> OPS[order-payment.js]
  SRV --> DSL[delivery-slots.js]
  SRV --> WCE[waterCalcEngine.js]

  DB --> PG[(PostgreSQL)]
  MAIL --> SMTP[(SMTP-сервер)]
```

---

## 6. Символьные схемы модулей

### 6.1. Аутентификация и почта

*Рисунок — схема работы модуля регистрации и подтверждения email.*

```mermaid
sequenceDiagram
  participant U as Пользователь
  participant B as script.js / cabinet-pg.js
  participant A as api-client.js
  participant S as server.js
  participant M as mailer.js
  participant D as PostgreSQL

  U->>B: Регистрация / «Отправить код»
  B->>A: POST /api/auth/register или send-email-verify-code
  A->>S: JSON + CSRF + cookie сессии
  S->>S: schemas.validate()
  S->>D: INSERT users, hash кода
  S->>M: sendVerificationCodeEmail()
  alt SMTP настроен
    M-->>U: Письмо с 6-значным кодом
  else SMTP нет (dev)
    M-->>S: код в консоль / devCode в ответе
  end
  U->>B: Ввод кода
  B->>S: POST confirm-email-code
  S->>D: email_verified = 1
```

**Основные функции**

| Файл | Функция | Назначение |
|------|---------|------------|
| server.js | `generateAuthCode`, `hashAuthCode`, `verifyAuthCode` | Генерация и проверка 6-значного кода |
| server.js | `sendEmailVerificationForUser` | Запись кода в БД + отправка письма |
| server.js | `sendPasswordResetForUser` | Код смены пароля на email |
| mailer.js | `sendVerificationCodeEmail`, `sendPasswordCodeEmail` | Текст/HTML письма |
| mailer.js | `isMailConfigured`, `verifySmtpConnection` | Проверка SMTP |
| schemas.js | `registerSchema`, `confirmEmailCodeSchema`, … | Валидация полей |
| script.js | обработчики auth-модалки | UI входа/регистрации |
| cabinet-pg.js | `applyEmailCodeDeliveryResponse` | Сообщения и dev-код в кабинете |

---

### 6.2. Оформление заказа (клиент)

```mermaid
flowchart TD
  A[Корзина script.js] --> B{Пользователь вошёл?}
  B -->|нет| C[Модалка входа]
  B -->|да| D[checkout: адрес, дата, слот]
  D --> E[maps-runtime + geocode API]
  D --> F[delivery-availability API]
  D --> G[POST /api/orders]
  G --> H[order-pricing: цены, бонусы]
  G --> I[bonusProgram: списание/начисление]
  G --> J[db: INSERT orders, −stock]
  G --> K[clientNotifications: уведомление]
  J --> L[Модалка успеха + orders-sync]
```

**Основные функции**

| Файл | Функция | Назначение |
|------|---------|------------|
| script.js | `renderCart`, checkout submit | Корзина и отправка заказа |
| order-pricing.js | `resolveStaffOrderItemsPatch`, расчёт строк | Цены и `items_json` |
| bonusProgram.js | `spendUserBonuses`, `accrueUserBonuses` | Бонусы по заказу |
| delivery-availability.js | `validateDeliveryBooking` | Закрытые дни/слоты |
| orenburg-address.js | `validateDeliveryAddressLine` | Адрес только Оренбург |
| orders-sync.js | `notifyOrdersDataChanged` | Синхронизация вкладок |

---

### 6.3. Личный кабинет клиента

```mermaid
flowchart LR
  CAB[cabinet.html] --> PG[cabinet-pg.js]
  PG --> API[api-client.js]
  API --> E1[GET /api/auth/me]
  API --> E2[GET /api/orders/my]
  API --> E3[GET /api/profile/addresses]
  API --> E4[GET /api/profile/bonus-program]
  API --> E5[PATCH /api/profile]
  API --> E6[auth: код email / пароль]
  PG --> NC[notifications-client.js]
  NC --> E7[GET /api/notifications/my]
```

**Основные функции `cabinet-pg.js`**

| Функция | Назначение |
|---------|------------|
| `renderProfile` | Отображение профиля |
| `renderOrders` / `renderOrderCard` | Активные доставки и история |
| `reloadOrders` | Опрос заказов каждые 10 с |
| `renderAddresses` | Сохранённые адреса CRUD |
| `renderBonusProgram` | Баланс, статус, история бонусов |
| `applyEmailCodeDeliveryResponse` | UX отправки кода email |

---

### 6.4. Уведомления на сайте

```mermaid
sequenceDiagram
  participant O as operator.js
  participant S as server.js
  participant N as clientNotifications.js
  participant DB as PostgreSQL
  participant C as notifications-client.js
  participant A as app-core.js

  O->>S: PATCH /api/orders/:id status
  S->>N: notifyOrderStatusChange()
  N->>DB: INSERT client_notifications
  loop каждые 12 с
    C->>S: GET /api/notifications/my
    S->>DB: SELECT уведомления
    C->>A: mergeServerNotifications()
    A->>A: колокольчик #topbarNotifications
  end
```

---

### 6.5. Панель оператора

```mermaid
flowchart TD
  OP[operator.html] --> OJS[operator.js]
  OJS --> LIST[GET /api/orders]
  OJS --> PATCH[PATCH /api/orders/:id]
  OJS --> CREATE[POST /api/orders/operator]
  PATCH --> AUDIT[order_audit_events]
  PATCH --> NOTIF[clientNotifications]
  OJS --> MAP[maps-runtime.js]
  OJS --> XLS[xlsx — отчёты]
```

---

### 6.6. Геокодирование

```mermaid
flowchart LR
  MR[maps-runtime.js] --> GP[geocode-proxy.js]
  GP --> Y[Yandex Geocoder API]
  GP --> N[Nominatim fallback]
  GP --> C[Кэш поиска]
```

---

### 6.7. База данных (`db.js`)

```mermaid
flowchart TD
  START[openDatabase] --> MIG[migrate: CREATE TABLE]
  MIG --> IDX[индексы, FK]
  MIG --> TBL[users, orders, products, bonus_operations, client_notifications, poll_votes, …]
  TBL --> POOL[Пул pg / обёртка prepare]
  POOL --> API[server.js запросы]
```

**Ключевые таблицы**

| Таблица | Назначение |
|---------|------------|
| users | Клиенты и сотрудники, роли, коды email/пароля |
| orders | Заказы, статус, доставка |
| products | Каталог, остатки |
| bonus_operations | Начисление/списание бонусов |
| client_notifications | Push-уведомления в колокольчик |
| client_saved_addresses | Адреса клиента |
| order_audit_events | Журнал изменений заказа |
| poll_votes | Голоса в опросах блога |

---

## 7. Карта REST API (укрупнённо)

```mermaid
mindmap
  root((API /api))
    Публичные
      products
      public/settings
      public/delivery-availability
      public/water-calc
      polls/vote
    Auth
      register login logout
      confirm-email-code
      forgot-password
      reset-password-by-email
    Клиент
      orders/my POST orders
      profile addresses
      bonus-program
      notifications/my
    Оператор
      orders PATCH
      orders/operator
      driver/orders
    Админ
      admin/users
      admin/products
      admin/stats
```

---

## 8. Полный перечень файлов по назначению

### HTML-страницы

| Файл | Роль |
|------|------|
| index.html | Главная, калькулятор воды, вход |
| catalog.html | Каталог и корзина |
| delivery.html | Условия доставки, FAQ |
| about.html | О компании, сертификаты |
| contacts.html | Контакты, обратная связь |
| community.html | Блог, опросы |
| cabinet.html | Личный кабинет |
| reset-password.html | Смена пароля по коду |
| verify-email.html | Редирект в кабинет |
| operator.html | Заказы, клиенты |
| manager.html | Контент, опросы, блог |
| admin.html | Пользователи, товары, зоны |
| driver.html | Маршрут водителя |

### Серверные модули (бизнес-логика)

| Файл | Кратко |
|------|--------|
| server.js | Маршруты API, сессии, оркестрация |
| db.js | Схема БД, миграции |
| schemas.js | Валидация Joi |
| mailer.js | Email SMTP |
| bonusProgram.js | Бонусная программа |
| clientNotifications.js | Уведомления клиента |
| pollVotes.js | Голосование в опросах |
| order-pricing.js | Расчёт суммы заказа |
| order-payment.js | Агрегация оплат в отчётах |
| delivery-availability.js | Календарь доставки |
| delivery-slots.js | Сравнение слотов |
| orenburg-address.js | Валидация адреса |
| geocode-proxy.js | Поиск адреса на карте |
| waterCalcEngine.js | Калькулятор потребления воды |
| security-middleware.js | Заголовки, rate limit |

### Клиентские модули

| Файл | Кратко |
|------|--------|
| script.js | Auth, корзина, checkout, каталог |
| cabinet-pg.js | Кабинет: профиль, заказы, бонусы |
| api-client.js | fetch, CSRF, JSON |
| app-core.js | Уведомления, localStorage, настройки |
| notifications-client.js | Синхронизация уведомлений с API |
| orders-sync.js | Обновление списка заказов между вкладками |
| maps-runtime.js | Яндекс.Карты, выбор адреса |
| mobile-nav.js | Мобильное меню |
| community.js | Блог и опросы |
| operator.js / admin.js / manager.js / driver.js | Панели staff |
| reset-password-page.js | Форма сброса пароля |

---

## 9. Рекомендуемые номера рисунков для диплома

| № | Содержание | Раздел документа |
|---|----------|------------------|
| 1 | Рис. 1 — Общая трёхзвенная архитектура | §1 |
| 2 | Рис. 2 — Процесс: клиент → оператор → водитель | §2 |
| 3 | Рис. 3 — Иерархия страниц и ролей | §3 |
| 4 | Рис. 4 — Зависимости клиентских JS | §4 |
| 5 | Рис. 5 — Модули сервера | §5 |
| 6 | Рис. 6 — Регистрация и email (sequence) | §6.1 |
| 7 | Рис. 7 — Оформление заказа | §6.2 |
| 8 | Рис. 8 — Личный кабинет | §6.3 |
| 9 | Рис. 9 — Уведомления | §6.4 |
| 10 | Рис. 10 — Панель оператора | §6.5 |
| 11 | Рис. 11 — Схема БД (таблицы) | §6.7 |

---

## 10. Текст для вставки в пояснительную записку (шаблон)

> Архитектура веб-приложения «ЭкваЛайн» построена по трёхзвенной модели (рисунок 1). Уровень представления — HTML-страницы и JavaScript в браузере; уровень логики — сервер Node.js (Express) с REST API `/api/*`, сессиями и CSRF; уровень данных — PostgreSQL и SMTP для писем с кодами подтверждения.
>
> Сквозной процесс (рисунок 2): клиент регистрируется и подтверждает email → оформляет заявку в каталоге → оператор обрабатывает заказ и назначает водителя → водитель отмечает доставку → клиент видит статус и уведомления в личном кабинете. Детальные схемы модулей и иерархия страниц приведены в §3–§6.

---

*Файл: `docs/АРХИТЕКТУРА-ВЕБ-ПРИЛОЖЕНИЯ.md` — обновляйте при добавлении новых страниц или API.*
